#include <stdio.h>

int main (void) {
    // unsigned int a; // unsighned : 항상 양수만 저장, %u로 출력

    // a = 4294967295;
    // printf("%d\n", a);
    // a = -1;
    // printf("%u\n", a);


    // char grade;
    // char name[20];

    // printf("학점 입력 : ");
    // scanf("%c", &grade); // 90은 문자열이기 때문에, 제대로 저장이 안된다.

    // printf("이름 입력 : ");
    // scanf("%s", name);

    // printf("%s의 학점은 %c입니다.\n", name, grade);


    
    // int a = 10, b = 10, c = 0;
    // c = a++;

    // printf("a : %d\n", a);
    // printf("c : %d\n", c);

    // int a = 20, b = 3;
    // double res;

    // res = ((double)a) / ((double)b);
    // printf("a = %d, b = %d\n", a, b);
    // printf("a / b의 결과 : %.1lf\n", res);

    // a = (int)res;
    // printf("(int) %.1lf의 결과 : %d\n", res, a);

    // int a = 10;
    // double b = 3.4;

    // printf("int형 변수의 크기 : %d\n", sizeof(a));
    // printf("double형 변수의 크기 : %d\n", sizeof(b));
    // printf("정수형 변수의 크기 : %d\n", sizeof(10));
    // printf("수식형 변수의 크기 : %d\n", sizeof(1.5 + 3.4));
    // printf("char형 변수의 크기 : %d\n", sizeof(char));

    // int a = 20;
    // int b = 0;

    // if (a > 10) {
    //     b = a;
    // }

    // printf("a : %d, b : %d\n", a, b);


    // int a = 1;

    // if (a == 1) {
    //     printf("일");
    // }
    // else if (a == 2) {
    //     printf("이");
    // }
    // else if (a == 3) {
    // printf("삼");
    // }
    // else if (a == 4) {
    // printf("사");
    // }
    // else if (a == 5) {
    // printf("오");
    // }
    // else {
    //     printf("육");
    // }

    return 0;
}